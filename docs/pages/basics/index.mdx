import Collapsible from "../../components/Collapsible";

import { Callout } from "vocs/components";

# Basics [An introduction to Binius]

_Binius_ is a state-of-the-art cryptographic proof system that allows fast and cheap verification of complex computations. The project comprises a high-performance, open-source [Rust implementation](https://github.com/IrreducibleOSS/binius) and a documentation suite on this very website. It builds directly on theoretical protocols published in a sequence of recent technical manuscripts [[DP23](https://eprint.iacr.org/2024/1038)] [[DP24](https://eprint.iacr.org/2024/504)]. This work is driven by [Irreducible's](https://www.irreducible.com/) research and development efforts.

In the world of cryptography, Binius is known as a succinct, noninteractive argument, or _SNARK_. Though Binius proofs today do not guarantee privacy, in the future, we plan to extend the system to provide zero knowledge proofs, making it a _zk-SNARK_. Because Binius uses binary fields as a foundational primitive, we also sometimes refer to it as a _binary proof system_[^1].

Binius's cryptographic protocol operates natively over _towers of binary fields_, mathematical objects that possess several remarkable properties. Binary towers efficiently arithmetize natural problems of interest, like standard hash functions, and also admit extremely efficient software and hardware implementations. The [Binius codebase](https://github.com/IrreducibleOSS/binius) contains, in addition to the core prover and verifier logic, various examples expressing computations of essential interest with Binius's [arithmetization scheme](/basics/arithmetization).

In this area of the site, we collect basic resources, designed to jump-start those planning to build on Binius. Here, we mainly focus on Binius's API and on the [arithmetization](/basics/arithmetization) scheme we use to capture real-life statements and problems. We defer Binius's cryptographic inner-workings to our [blueprint](/blueprint) section.

## Getting Started

For some general introductory content, start with our [overview page](/basics/overview). [Roadmap page](/basics/roadmap) lists what we plan to add to Binius64 in the future.

## Site Directory

Below, we sketch this site area's sections in slightly more detail. Here, we focus on _usage_ and on basic concepts, deferring all cryptographic content to our [blueprint](/blueprint).

<Callout type="note">
<Collapsible>
[**Binius's Arithmetization.**](arithmetization) In this section, we define abstract tools and objects which let us efficiently capture _meaningful_ real-life statements and problems.

Binius introduces a new arithmetization scheme, called _M3_ (short for _multi-multiset matching_). M3 features a plurality of _tables_, to each of which arbitrary polynomial _gate constraints_—operating row-wise—may be imposed. We moreover link and connect these tables using a new abstraction we call _channels_. Channels are abstract objects, _to_ and _from_ which tuples of data may be "pushed" and "pulled". Our arithmetization paradigm encompasses _not just_ a plurality of tables (each, again, bearing gate constraints), but moreover so-called _flushing rules_, which dictate how elements from these tables should be pushed to and pulled from channels. A channel is said to be _balanced_ when the pushes made to it exactly match the pulls made from it.

As it turns out, many essential problems, like [Merkle–Patricia trie inclusion](/basics/arithmetization/mpt), can be captured as M3 instances. For more on arithmetization, start [here](/basics/arithmetization).

</Collapsible>
</Callout>

<Callout type="note">
<Collapsible>
[**Further Resources.**](resources) Here, we collect various Binius-related resources from the broader internet.

Indeed, we lay out separate sections for Binius-related Irreducible blog posts, talks given by Irreducible team members, and external content.

</Collapsible>
</Callout>

[^1]: h/t Weikeng Chen for coining the term "binary proof".
